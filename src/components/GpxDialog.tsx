import { useEffect, useState } from "react";
import { downloadText } from "../application/backupService";
import { exportGpx, parseGpx } from "../application/gpxService";
import { listRouteTracks, saveImportedGpx } from "../application/routeTrackService";
import { listWaypoints } from "../application/waypointService";
import type { RouteTrack, Waypoint } from "../domain/models";

export function GpxDialog({ tripId, onClose }: { tripId: string; onClose: () => void }) { const [waypoints, setWaypoints] = useState<Waypoint[]>([]); const [routes, setRoutes] = useState<RouteTrack[]>([]); const [message, setMessage] = useState(""); const refresh = async () => { const [nextWaypoints, nextRoutes] = await Promise.all([listWaypoints(tripId), listRouteTracks(tripId)]); setWaypoints(nextWaypoints); setRoutes(nextRoutes); }; useEffect(() => { let active = true; void Promise.all([listWaypoints(tripId), listRouteTracks(tripId)]).then(([nextWaypoints, nextRoutes]) => { if (active) { setWaypoints(nextWaypoints); setRoutes(nextRoutes); } }); return () => { active = false; }; }, [tripId]); async function importFile(file?: File) { if (!file) return; try { const parsed = parseGpx(await file.text(), tripId); await saveImportedGpx(parsed.waypoints, parsed.routes); await refresh(); setMessage("GPX imported locally."); } catch (error) { setMessage(error instanceof Error ? error.message : "Could not import GPX."); } } return <div className="dialog-backdrop" role="presentation"><section className="dialog" role="dialog" aria-modal="true" aria-label="GPX routes and tracks"><div className="dialog-heading"><h2>GPX routes & tracks</h2><button className="text-button" aria-label="Close dialog" type="button" onClick={onClose}>×</button></div><div className="data-actions"><label className="secondary-action">Import GPX<input type="file" accept=".gpx,application/gpx+xml" onChange={(event) => void importFile(event.currentTarget.files?.[0])} /></label><button className="primary-action" type="button" onClick={() => downloadText("trip.gpx", exportGpx(waypoints, routes), "application/gpx+xml")}>Export GPX</button></div>{message && <p className="error-message" role="status">{message}</p>}<ul className="item-list">{routes.map((route) => <li className="inventory-item" key={route.id}><div><strong>{route.name}</strong><small>{route.kind} · {(route.distanceMeters / 1000).toFixed(1)} km · {route.points.length} points</small></div></li>)}</ul><p className="empty-state">{waypoints.length} local waypoints · {routes.length} routes/tracks. GPX export excludes private trip notes and medical details.</p></section></div>; }
