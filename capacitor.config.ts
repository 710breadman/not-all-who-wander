import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.notallwhowander.camping",
  appName: "Path A Logical",
  webDir: "dist",
  server: { androidScheme: "https" },
};

export default config;
